export default defineAppConfig({
  ui: {
    colors: { primary: "amber", neutral: "neutral" },
    button: {
      slots: { base: "cursor-pointer" },
    },
    select: {
      slots: { base: "cursor-pointer" },
    },
  },
});
